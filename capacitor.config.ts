import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rhen.fashionlanding',
  appName: 'fashion-landing',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;